name: Benchmark

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v6

      # 2. Install k6
      - name: Install k6
        run: |
          sudo apt-get update
          sudo apt-get install -y gnupg ca-certificates
          sudo mkdir -p /etc/apt/keyrings
          curl -fsSL https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /etc/apt/keyrings/k6.gpg
          echo "deb [signed-by=/etc/apt/keyrings/k6.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6

      # 3. Start services
      - name: Build and start services
        run: docker compose up -d --build

      # 4. Wait for services ready
      - name: Wait for services
        run: |
          echo "Waiting for app..."
          for i in $(seq 1 60); do
            if curl -sf --ipv4 http://localhost:3000/movies > /dev/null 2>&1; then
              echo "App ready!"
              break
            fi
            echo "Attempt $i/60 - not ready yet..."
            docker compose logs --tail=3 app
            if [ $i -eq 60 ]; then
              echo "TIMEOUT"
              docker compose logs app
              docker compose logs db
              exit 1
            fi
            sleep 5
          done

          echo "Waiting for Prometheus..."
          for i in $(seq 1 20); do
            if curl -sf --ipv4 http://localhost:9090/-/ready > /dev/null 2>&1; then
              echo "Prometheus ready!"
              break
            fi
            echo "Attempt $i/20..."
            sleep 3
          done

      # 5. Setup Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 6. Install dependencies
      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # 7. Run load test (k6 via python)
      - name: Run benchmark test
        run: python metrics/run-test.py

      # 8. Kasih waktu Prometheus scrape data terakhir
      - name: Wait before exporting metrics
        run: sleep 10

      # 9. Export metrics ke CSV
      - name: Export metrics
        run: python metrics/export-metrics.py

      - name: Clean k6 Response Time Data
        run: python metrics/clean-data.py

      # 10. Upload hasil benchmark
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            *.csv
            *.png
